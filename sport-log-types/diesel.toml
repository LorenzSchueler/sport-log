# For documentation on how to configure this file,
# see diesel.rs/guides/configuring-diesel-cli

[print_schema]
file = "src/schema.rs"
import_types = ["diesel::sql_types::*", "crate::*"]
filter = { except_tables = ["platform_archive"] }
patch_file = "src/schema.patch"
